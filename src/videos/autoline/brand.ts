/**
 * Public-facing product identity shown on screen. The hero wordmark renders as
 * `<pre><post>` with `post` accent-colored, e.g. Auto·Line.
 */
export const BRAND = {
  pre: "Auto",
  post: "Line",
  name: "AutoLine",
  course: "MASTERCLASS",
  hero: ["FULLY AUTOMATED", "VIDEO PRODUCTION LINE"] as const,
  tagline: "Record once — the pipeline cuts, re-voices and renders the rest.",
};
